import { NavMenuType } from "../types";
type SidebarProperties = {
    children?: React.ReactNode;
    displayNavIcons?: boolean;
    navigationMenu?: NavMenuType;
    noHeader?: boolean;
    noFooter?: boolean;
    noLocaleSwitcher?: boolean;
};
export declare const Sidebar: ({ children, displayNavIcons, navigationMenu, noHeader, noFooter, noLocaleSwitcher, }: SidebarProperties) => import("react").JSX.Element;
export {};
//# sourceMappingURL=Sidebar.d.ts.map