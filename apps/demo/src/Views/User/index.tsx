import ComponentList from "../../components/ComponentList";

const User = () => {
  return (
    <ComponentList
      componentList={[]}
      title="header.menu.user"
      translationNameSpace={["user"]}
    />
  );
};

export default User;
